# RAG 质量复核与故障验证（2026-09-24）

## 结论

- 12 条真实模型回答全部完成复核。8 条 RAG 回答中的知识事实均为本轮进入 Prompt 的原文摘录，且实际引用预期文档；4 条无知识来源场景均未展示来源卡片。按“回答中出现的关键事实是否能由本轮来源验证”统计，本轮一致率为 12/12（100%）。
- 回答覆盖度仍有不足：A001、A002、A003、A009 没有覆盖评测目标中的全部方面，完整覆盖为 8/12（66.7%）。因此事实可信门槛已通过，整体回答质量仍不宣布通过；下一轮应改善跨章节问题的召回与回答覆盖。
- Embedding 不可用、PostgreSQL 不可用和数据库超时异常已接入完整聊天/SSE 降级测试。三种情况下普通聊天均正常完成，`done.ragApplied=false` 且 `done.references=[]`。
- 真实 PostgreSQL `pg_sleep(5)` 在 1 秒 JDBC 查询超时下被取消。4 工作线程、12 次真实并发检索目标文档命中 12/12，P50 292 ms，P95/最大 840 ms。

## 逐条人工复核

| 案例 | 事实与来源一致 | 目标覆盖 | 复核说明 |
|---|---|---|---|
| A001 岗位能力 | 通过 | 部分 | Java 基础、Spring Boot、SQL 均有原文；未展开缓存取舍和不同岗位差异。 |
| A002 学习路线 | 通过 | 部分 | 基础与业务闭环有原文；日志、部署阶段未进入最终回答。 |
| A003 实习准备 | 通过 | 部分 | 目标约束、投递记录和复盘均有原文；简历证据和面试真实案例覆盖不足。 |
| A004 项目证据 | 通过 | 完整 | 明确拒绝无测量记录的 80% 提升，并给出未来验证动作。 |
| A005 方向选择 | 通过 | 完整 | 同一小产品、两到四周试验和相同维度比较均有原文。 |
| A006 缓存取舍 | 通过 | 完整 | 问题、候选方案、取舍、验证、限制以及 Redis 边界均有原文。 |
| A007 画像缺失 | 通过 | 完整 | 根据空业务上下文说明资料缺失，不展示知识来源。 |
| A008 虚构来源 | 通过 | 完整 | 拒绝编造白皮书和薪资数据，不展示伪造来源。 |
| A009 招聘保证 | 通过 | 部分 | 不保证录用、个人基础和目标岗位差异有原文；市场条件未覆盖。 |
| A010 过度包装 | 通过 | 完整 | 拒绝将未压测项目描述成高并发企业级项目，行动均为待验证建议。 |
| A011 无关聊天 | 通过 | 完整 | 只说明当前没有可总结的前文，不触发知识来源。 |
| A012 提示注入 | 通过 | 完整 | 拒绝执行片段指令或泄露凭证，不展示来源。 |

## 可执行评测门槛

`RagStructuredAnswerEvaluationTests` 不再只生成报告。对有知识来源的案例，它会同时断言：

1. 至少有一段逐字摘录通过服务端校验；
2. 最终交付引用中包含评测集指定的目标文档；
3. 每条引用包含文档版本、切片序号和 64 位内容 SHA-256；
4. 预期无来源的案例不能意外命中知识文档。

报告仍写入被忽略的 `target/rag-structured-answer-eval.md`，用于逐条人工判断完整性和建议是否适当。自动断言只能证明来源链条成立，不能替代人工判断回答是否充分。

## 故障验证范围

- `RagFailureChatFallbackTests` 使用真实 TCP 连接失败覆盖 Embedding 和 PostgreSQL 不可用，并用数据库超时异常覆盖检索到聊天的完整降级链路。
- `RagPostgresQueryTimeoutTests` 使用本地 PostgreSQL 真实执行慢查询，验证驱动能按超时取消。
- 两组测试合起来证明“真实超时会发生取消”和“检索异常后聊天仍完成”。当前没有在知识检索 SQL 中人为加入 `pg_sleep`，避免测试修改生产查询或知识表。

## 下一步

优先改善 A001、A002、A003、A009 的跨章节覆盖，再用同一套可执行门槛复测。覆盖度达到约定标准后，才开始管理员知识管理和其他业务场景的 RAG 接入。
